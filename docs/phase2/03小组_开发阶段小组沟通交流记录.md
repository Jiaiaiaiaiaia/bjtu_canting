# 03 小组 开发阶段沟通交流记录

**项目**：北京交通大学就餐仿真系统
**阶段**：开发阶段（第 5–9 周）
**记录人**：朱思思（轮值）

---

## 模板（每次沟通新建一个表）

| 项目 | 内容 |
|------|------|
| 沟通时间 | YYYY-MM-DD HH:MM |
| 沟通方式 | 线下 / 微信群 / 腾讯会议 |
| 参与成员 | 朱思思、宋嘉桐、贾文霞 |
| 沟通主题 | （一句话） |
| 议定事项 | 1. ...<br>2. ... |
| 未达成共识事项 | （及计划解决方式） |
| 后续动作 | 责任人 + 截止日期 |

---

## 第 1 次沟通 —— 启动会

| 项目 | 内容 |
|------|------|
| 沟通时间 | 2026-04-01 19:30 |
| 沟通方式 | 线下（图书馆 5 楼研讨间） |
| 参与成员 | 朱思思、宋嘉桐、贾文霞 |
| 沟通主题 | 开发阶段启动：技术栈、模块划分、协作流程 |
| 议定事项 | 1. 技术栈：后端 Python 3.12 + Flask + SQLite；前端原生 HTML/CSS/JS + ECharts + Canvas 2D；不引入额外框架以降低环境复杂度。<br>2. 架构：B/S，后端跑在 5001 端口（macOS 5000 被 AirPlay 占用）。<br>3. 模块划分按角色硬切，靠 REST API 解耦。<br>4. 一周一次例会（周三晚），关键问题随时拉语音。 |
| 未达成共识事项 | 是否引入身份登录 —— 决定先不做，留扩展空间。 |
| 后续动作 | 朱：本周完成仿真引擎 v0；宋：本周完成 HTML 骨架与样式；贾：标定默认参数 |

---

## 第 2 次沟通 —— 接口联调约定

| 项目 | 内容 |
|------|------|
| 沟通时间 | 2026-04-08 19:30 |
| 沟通方式 | 线下 |
| 参与成员 | 朱思思、宋嘉桐、贾文霞 |
| 沟通主题 | 后端 API 字段约定，明确前端 step 的轮询节奏 |
| 议定事项 | 1. `step` 接口每次返回完整状态（windows / seats / students 三个数组），前端按需取用。<br>2. 前端使用 `setInterval(500/speed)` 轮询，speed ∈ {1,2,5,10}。<br>3. 座位热力图通过 `seats[i].remaining_time` 着色。<br>4. 数据库表先固化两张：`simulation_config`、`simulation_snapshot`，避免阶段间频繁迁移。 |
| 未达成共识事项 | 学生流动动画是否做"按学生 ID 追踪"——分歧在于性能。结论：先做最简实现（聚合圆点），如果分析阶段评测结果不够直观再升级。 |
| 后续动作 | 朱：把 9 个接口的 JSON schema 整理一份给宋；贾：跑 3 组典型参数，记录系统响应时间 |

---

## 第 3 次沟通 —— 中期问题清单

| 项目 | 内容 |
|------|------|
| 沟通时间 | 2026-04-15 20:00 |
| 沟通方式 | 腾讯会议 |
| 参与成员 | 朱思思、宋嘉桐、贾文霞 |
| 沟通主题 | 第 7 周 review，集中解决目前发现的 5 个核心 bug |
| 议定事项 | 1. `total_arrived` 在 `start()` 阶段被一次性预统计，导致界面一上来就显示最终人数 —— 朱负责修：把累加移到 `_handle_arrival`。<br>2. `/simulation/start` 可重复调用，会再次预生成到达事件 —— 加 `_is_started` 守卫。<br>3. "结束仿真"按钮目前只是 pause —— 改造为新接口 `/simulation/finish`，后端 fast-forward 到结束。<br>4. 座位 `remaining_time` 是入座时锁定的静态值，没有递减 —— 改用 `eat_end_time`，渲染时按 `current_time` 动态换算。<br>5. README 写了"可查历史"，前端缺入口 —— 宋负责加第 4 个 nav 与历史页。 |
| 未达成共识事项 | 无。 |
| 后续动作 | 5 个修复本周内全部合入，朱本周末跑一遍 pytest 确认；宋同步增加学生 ID 级别的圆点动画；贾负责把单元测试结果整理进单元测试报告 |

---

## 第 4 次沟通 —— 单元测试与文档

| 项目 | 内容 |
|------|------|
| 沟通时间 | 2026-04-19 21:00 |
| 沟通方式 | 微信群 |
| 参与成员 | 朱思思、宋嘉桐、贾文霞 |
| 沟通主题 | 单元测试覆盖率确认 + 第二阶段交付物分工 |
| 议定事项 | 1. 后端 pytest 38 条全部通过，覆盖三层模块 + API；测试报告由朱出第一版。<br>2. 前端依赖手动测试：宋按"参数提交→运行→结束→历史"四个金路径过一遍并截图。<br>3. 第二阶段交付物清单：团队 2 份 + 个人 3 份/人，截止 5 月 3 日。<br>4. 文档统一 markdown 起草，最终用 pandoc 转 PDF。 |
| 未达成共识事项 | 无。 |
| 后续动作 | 4/22 前各自完成本人的开发阶段实训报告草稿，4/26 互评，4/30 转 PDF，5/2 提交 |

---

> 实际沟通发生时请补充真实时间、地点、议定细节。本文件按时间倒序保留 4 次最近会议为示例。
